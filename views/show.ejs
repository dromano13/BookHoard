<!DOCTYPE html>
<html>
    <head></head>

    <body>
        <h1><%=book.title%></h1>

        <img src=<%=book.img%> />

        <% if (book.userId === true) { %>
        This tome was added to the Hoard by <%=book.User.name%>
        <% } else { %>
            A Tome of Wonder
        <% } %>
        
        <h3>It is available in the following formats:</h3>
        <select name="formats">
            <% for ( let i = 0; i < formats.length; i++ ) {
                let selected = ( i == 0 ) ? "selected" : "";
            %>
                <option value="<%=formats[i].id%>" <%=selected%>><%=formats[i].name%>
                </option>
           <% } %>
        </select>
    
            <br>
    </body>
</html>