<!DOCTYPE html>
<html>
    <head></head>
    <body>
        <nav>
            <a href="/" class="f90-logout-button">Logout</a>
        </nav>
        <h1><%=book.title%></h1>
        <img src=<%=book.img%> />
        This tome was added to the Hoard by <%=book.User.name%><br>
        <h3>It is available in the following formats:</h3>
        <select name="formatId">
            <% for ( let i = 0; i<formats.length; i++ ) {
                let selected = (i == 0) ? "selected" : "";
            %>
                <option value="<%=formats[i].id%>" <%=selected%>><%=formats[i].name%></option>
           <% } %>
        </select>
        <br>
    </body>
</html>